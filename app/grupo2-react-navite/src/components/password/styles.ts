import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  passwordContainer: {
    width: '100%',
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 20,
  },
  eyeIcon: {
    position: 'absolute',
    right: 10,
    color: '#f0f0f0',
    top: '30%',
  },
});

export default styles;
