import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    button: {
        backgroundColor: '#fff',
        paddingVertical: 10,
        paddingHorizontal: 20,
        borderColor: '#000',
        borderRadius: 10,
        borderWidth: 1,
    },
    buttonText: {
        color: '#000',
        fontSize: 20,
    },
})